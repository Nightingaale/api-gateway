spring:
  config:
    import: vault://secret/prod/api-gateway
  cloud:
    vault:
      uri: http://vault:8200
      authentication: token
      token: root
      kv:
        enabled: true
        backend: secret
        application-name: api-gateway
      enabled: true